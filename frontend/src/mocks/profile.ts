/** Mock signed-in user — no backend/auth system in scope for this issue, so
 * this is the one fixed "current user" the app shows once mock-signed-in. */
export const MOCK_PROFILE = {
  name: 'Eduarda Souza',
  email: 'eduarda.souza@exemplo.com',
};
